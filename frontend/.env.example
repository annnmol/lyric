# ─── Frontend config ───────────────────────────────────────────
# Vite exposes vars prefixed with VITE_ to the browser via import.meta.env.
# All values are optional — defaults work for local development.

# Base URL of the backend API (Express server)
VITE_API_URL=http://localhost:8765
